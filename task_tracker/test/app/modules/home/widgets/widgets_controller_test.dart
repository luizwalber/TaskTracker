import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:task_tracker/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // WidgetsController widgets;
  //
  setUp(() {
    //     widgets = HomeModule.to.get<WidgetsController>();
  });

  group('WidgetsController Test', () {
    //   test("First Test", () {
    //     expect(widgets, isInstanceOf<WidgetsController>());
    //   });

    //   test("Set Value", () {
    //     expect(widgets.value, equals(0));
    //     widgets.increment();
    //     expect(widgets.value, equals(1));
    //   });
  });
}
